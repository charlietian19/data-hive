<% project.research_applications.where(status: 2).each do |application| %>
    <%= application.user.email %>
    <% qnumber = 0 %>
    Questions and answers </br>
    <% application.answers.each do |answer|%>
      <%= project.questions[qnumber]%>
      <%= answer %> </br>
      <% qnumber += 1 %>
    <% end %>
<% end %>

<!-- the accepted_application_ids need to be handled by some JS so the request probably should be an AJAX call -->
<!-- <%= link_to "Send Accept Messages", accept_project_path(id: project, accpeted_application_ids: [1,2,3]), method: :put %> -->
