projects index </br>
<% @projects.each do |p| %>
  <%= p.title %> <%= link_to p.title, project_path(p) %> </br>
<% end %>


<%= link_to 'new project', new_project_path%>
<% if user_signed_in? %>
  <%= link_to 'dashboard', user_dashboard_path(current_user)%>
<%end%>

